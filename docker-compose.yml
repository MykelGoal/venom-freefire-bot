version: '3.8'

services:
  venom-freefire:
    build: .
    container_name: venom-freefire-bot
    restart: always
    ports:
      - "3000:3000"
    environment:
      - BOT_TOKEN=${BOT_TOKEN}
      - PORT=3000
      - APP_URL=${APP_URL}
